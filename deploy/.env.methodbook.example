# Cross-competition Kaggle methodology learning.
# Relative METHODBOOK_DIR is resolved under PROJECT_ROOT and should be persisted.
METHODBOOK_ENABLED=true
METHODBOOK_DIR=knowledge

# Use the configured Codex/OpenAI provider to generalize each completed iteration.
# When false or unavailable, deterministic memo generation remains active.
ITERATION_MEMO_PROVIDER_ENABLED=true
